#include "main.h"
#include <stdlib.h>

/**
*/

char *_strdup(char *str)
{
int i;

if (str = nul)
{
return (NULL);
}
for(i = 0; i != '\0'; i++
gr = malloc(size * sizeof((str)+1));
if (gr == NULL)
{
return (NULL);
}

}
